{
  "name": "{{ name }}",
  "version": "0.0.1",
  "description": "{{ description}}",
  "author": "Skedulo",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "husky": "^7.0.2",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "lint-staged": "^15.4.3",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.5.3",
    "ts-jest": "^29.2.6",
    "typescript": "^5.8.2"
  },
  "jest-junit": {
    "suiteName": "jest tests",
    "outputDirectory": "reports",
    "outputName": "jest-junit.xml",
    "uniqueOutputName": "false",
    "classNameTemplate": "{classname}-{title}",
    "titleTemplate": "{classname}-{title}",
    "ancestorSeparator": " › ",
    "usePathForSuiteName": "true"
  },
  "lint-staged": {
    "**/*.{ts,tsx,css,scss,md,js,jsx}": [
      "prettier --write"
    ]
  },
  "scripts": {
    "bootstrap": "yarn install --frozen-lockfile --audit --ignore-scripts && ./.template/make-dirs.sh && yarn husky install && node .template/template.mjs",
    "lint": "npm-run-all lint:staged",
    "lint:staged": "lint-staged",
    "check:prettier": "prettier --check .",
    "check:prettier:fix": "prettier --write .",
    "prepare": "husky"
  },
  "dependencies": {
    "nunjucks": "^3.2.4",
    "prompt": "^1.3.0"
  }
}
